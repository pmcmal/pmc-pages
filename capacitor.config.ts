import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.hoppingkiwiswoop',
  appName: 'PSS Generator',
  webDir: 'dist'
};

export default config;