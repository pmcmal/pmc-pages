import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.hoppingkiwiswoop',
  appName: 'hopping-kiwi-swoop',
  webDir: 'dist'
};

export default config;
